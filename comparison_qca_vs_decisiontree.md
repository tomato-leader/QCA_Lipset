# QCA vs 決定木分析：決定木ルール「DEV≤0.46→非生存」の一貫性検証

## 1. 決定木のルールの整理

### 決定木が発見したルール
決定木分析（CART）から抽出される主要な分類ルール：
- **ルール1（否定ケース）**: `DEV ≤ 0.46` → 非生存（negative outcome）
- **ルール2（肯定ケース）**: `DEV ≥ 0.46` かつ `STB ≥ 0.51` → 生存（positive outcome）

このルールは、n=18という少数ケースに対して、
- 経済発展度（DEV）を**第一分岐変数**として選定
- その後、政府安定性（STB）による**第二分岐**を実施
していることを示唆している。

---

## 2. QCA分析の結果との対比

### QCA必要条件分析の知見
（セクション4の分析より）

| 条件 | 一貫性(inclN) | カバレッジ(covN) | 解釈 |
|-----|--------------|----------------|------|
| DEV | ~0.9+ | - | 必要条件の候補（一貫性が0.90に接近/超過） |
| LIT | 高い | 中程度 | **強い必要条件** |
| STB | 高い | 中程度 | **必要条件の一つ** |
| URB | 中程度 | 低い | trivially necessary の可能性 |
| IND | 低〜中程度 | 低い | 必要条件の証拠不十分 |

**QCA必要条件の解釈**：
- 生存した国は**必ずDEVが高い**傾向（必要条件性）
- 同時に**LITとSTBも必須要因**
- ただし単独では十分条件ではない

### QCA十分条件分析の知見
（セクション5の分析より）

| 解 | 論理式（簡略） | inclS | covS | 解釈 |
|----|--------------|-------|------|------|
| 解1 | DEV × LIT × URB × IND × STB | ~0.90 | ~0.39 | 全条件が揃った場合は確実に生存 |
| 解2 | DEV × ~URB × LIT × ~IND × STB | ~0.90 | - | 異なる発展経路（都市化を欠く） |

**QCA十分条件の解釈**：
- 生存を**保証する条件は複雑**（複数条件の組み合わせ必須）
- **等結果性**：複数の経路が存在（解1と解2）
- 全体カバレッジは65.8%（つまり、この2経路で説明できない生存ケースも存在）

---

## 3. 類似点（一貫性の証拠）

### ✅ 3.1 経済発展度（DEV）の重要性

**決定木の視点**：
- DEV を**第一分岐変数**として選定 → 予測精度を最大に高める変数
- DEV ≤ 0.46で「確実に非生存」と判定

**QCA の視点**：
- DEV は生存国でほぼ必ず高い（必要条件一貫性 > 0.90）
- つまり「DEVが低い = 民主主義が生存していない」という必要条件

**一貫性評価**: ⭐⭐⭐⭐⭐ **高度に一貫**
- 両手法とも DEV の**単独での区分力が強い**と判定
- DEV ≤ 0.46 という閾値は、QCA の必要条件（DEVが高いことが必須）と逆向きで一致

### ✅ 3.2 政府安定性（STB）の補完的役割

**決定木の視点**：
- DEV ≥ 0.46 のグループ内で、STB ≥ 0.51 が生存の**追加条件**
- つまり「DEVだけでは不十分。STBも必要」

**QCA の視点**：
- STB は生存国で高い傾向（必要条件一貫性が高い）
- 十分条件解に**STBが共通項**として含まれる（解1: DEV×LIT×URB×IND×STB、解2: DEV×~URB×LIT×~IND×STB）
- 両解とも STB を必須条件として含む

**一貫性評価**: ⭐⭐⭐⭐ **中〜高度に一貫**
- 決定木の「STB ≥ 0.51」という閾値は、QCA の「STBが十分条件に常に含まれる」という観察と合致
- ただし QCA では STB 単独では必要条件（inclN > 0.90）ではなく、他条件との組み合わせ（DEV×STB など）の中で機能

---

## 4. 差分（乖離の証拠）

### ❌ 4.1 識字率（LIT）の消失

**QCA の知見**：
- LIT は**最も強い必要条件**の一つ
- すべての十分条件解に包含される（解1の DEV×**LIT**×URB×IND×STB、解2の DEV×~URB×**LIT**×~IND×STB）
- 非生存の十分条件「~DEV × ~LIT」にも含まれる

**決定木の結果**：
- LIT が分岐変数として**選ばれていない**（重要度が低い？）
- n=18 という少数ケースで、LIT のファジィスコアが DEV や STB ほど明確な区分力を持たない可能性

**差分の理由（手法上）**：
1. **ファジィ化と離散化の違い**
   - QCA：LIT のファジィスコア（0〜1 の連続値）を連続的に扱う → 微妙な差異を捉える
   - 決定木：LIT を特定の閾値で二値化 → 分岐がない可能性（すべてのケースが一方に集中）

2. **サンプルサイズの影響**
   - n=18 で 4 変数のツリーを構築すると、後ろの層の分岐は不安定になる傾向
   - 決定木は複雑性ペナルティ（cp パラメータ）により、有効な分岐を限定

**リスク評価**: ⚠️ **決定木が過度に簡潔である可能性**
- 「DEV と STB だけで説明できる」という決定木の結論は、QCA の「LIT も必須」という知見と矛盾
- n=18 という少数ケースでは、決定木が実際の複雑性を見落とす可能性が高い

---

### ❌ 4.2 等結果性（複数経路）の非表現

**QCA の知見**：
- 生存を導く**2つの異なる十分条件経路**が存在（解1と解2）
- 解1：都市化 × 工業化を含む「完全な近代化パス」
- 解2：都市化 × 工業化を含まない「選別的発展パス」
- 両方とも DEV・LIT・STB を共通に含むが、URB と IND の組み合わせが異なる

**決定木の結果**：
- 単一の分岐ツリーで「最も効率的な」単一経路のみを提示
- 「DEV と STB だけで分類」という結論は、**等結果性を無視**している

**差分の理由（方法論的）**：
1. **最適化の目標の違い**
   - QCA：「どの条件組み合わせが十分条件か」を**すべて列挙**（集合論的アプローチ）
   - 決定木：「最大の予測精度」を達成する**単一ツリー**を選択（再帰的分割）

2. **因果ロジックの違い**
   - QCA：「X→Y または Z→Y」の両方が成立し得る（非対称性を認める）
   - 決定木：すべてのケースを**唯一の分類ツリー**に統合（対称的な分類）

**リスク評価**: ⚠️ **歴史的多様性の喪失**
- 現実には「都市化した発展経路」と「都市化しない発展経路」で民主主義が生存している
- 決定木がこれを一つの分岐に圧縮することで、歴史的文脈が消失

---

### ❌ 4.3 非対称性の扱い

**QCA の知見**：
- SURV（生存）と ~SURV（非生存）を**別々に分析**
- 生存の十分条件と非生存の十分条件が単純な論理否定ではない（非対称性）
- 例：「~DEV × ~LIT」が非生存の十分条件であっても、「DEV × LIT」が生存の十分条件とは限らない

**決定木の結果**：
- 二値分類として「生存 vs 非生存」を対称的に扱う
- DEV ≤ 0.46 → 非生存、DEV ≥ 0.46 → 生存... という**反転対称的な解釈**が暗黙的

**差分の理由（本質的）**：
- 決定木のジニ係数は対称的な分類指標 → 両カテゴリを等価に扱う
- QCA の集合論は非対称的 → X ⊆ Y と ~X ⊆ ~Y は独立に判定

---

## 5. 手法上の違いが乖離をもたらす理由

### 📊 5.1 サンプルサイズと汎化性能

| 側面 | QCA（n=18） | 決定木（n=18） |
|-----|-----------|-------------|
| **設計目標** | 小サンプルの因果パターン抽出に最適 | サンプルが多いほど精度向上 |
| **過学習のリスク** | 低い（集合論的統合） | **高い**（ツリーの深さが深くなると不安定） |
| **変数の取り扱い** | すべての変数を並列に評価 | 再帰的に「最も区分力のある変数」を優先 |
| **結果の安定性** | 一般的に堅牢 | n=18 では危険 |

**結論**：決定木は n=18 で不安定。BootstrapやCross-validationなしで信頼性に疑問。

---

### 📊 5.2 ファジィ化 vs 離散化

#### QCA のアプローチ：ファジィ化
```
入力：DEV = 550 USD（raw value）
↓ ファジィ化（クロスオーバー 550 USD）
→ DEV = 0.5（正確に中点）
  または 0.0〜1.0 の連続スコア
↓ 集計：min-min 演算で論理積を表現
```

**利点**：
- 「ちょうど 550 USD」といった境界ケースを 0.5 で表現
- 連続性を保持したまま論理操作

**制限**：
- クロスオーバー点の設定が任意的（理論的根拠が必要）

#### 決定木のアプローチ：離散化
```
入力：DEV = 0〜1 の連続スコア
↓ 最適分岐点を自動探索（Gini係数最小化）
→ DEV ≤ 0.46 vs DEV > 0.46
  （ただし、このカットオフは「生データ」ではなく
   「計算によって決まる」）
```

**利点**：
- カットオフが「データ駆動的」に決定
- 予測的には効率的

**制限**：
- 1 回の二値化により情報損失
- QCA の連続的なファジィ関係が表現できない

**具体例**：
| DEV スコア | QCA（ファジィ） | 決定木（離散） |
|-----------|---------------|--------------| 
| 0.40 | 0.40（約40% の生存確率） | 非生存 |
| 0.46 | 0.46（約46% の生存確率） | **分岐点** ←違い |
| 0.50 | 0.50（中立） | 生存 |
| 0.60 | 0.60（約60% の生存確率） | 生存 |

→ QCA は境界ケース（0.40〜0.50）の グラデーション を扱うが、決定木は**閾値で二分**

---

### 📊 5.3 論理構造：AND vs. OR

#### QCA：複数経路の AND-OR 論理
```
SURV ← (DEV × LIT × URB × IND × STB)
        OR
        (DEV × LIT × ~URB × ~IND × STB)
```

**含意**：
- 「この経路 **または** この経路」で生存
- 複数経路の等結果性を明示的に表現

#### 決定木：階層的 IF-THEN
```
IF DEV ≤ 0.46:
    → 非生存（決定）
ELSE IF DEV ≥ 0.46 AND STB ≥ 0.51:
    → 生存（決定）
ELSE:
    → [第3層の判定]
```

**含意**：
- 「ツリーを下る経路が唯一」
- 複数経路が存在する場合、ツリーはそれらを「混在させた単一ルール」に圧縮

---

### 📊 5.4 集合論 vs. 統計学

| 基本思想 | QCA | 決定木 |
|----------|-----|--------|
| **根拠** | 集合論（X ⊆ Y） | 統計学（分布、不確定性） |
| **一貫性の意味** | 「集合 X がほぼ集合 Y に内包される割合」 | 「ノード内で多数派が単一カテゴリ」 |
| **因果性** | 条件と結果の集合関係を直接的に記述 | 確率的な予測関係（相関） |
| **サンプル外推論** | 論理式は理論的に移転可能 | ツリー構造は対象データに特化（汎化困難） |

**結論**：QCA は「理論生成」志向、決定木は「分類精度」志向

---

## 6. 総括と考察

### 🎯 主な発見

| 項目 | 一貫性 | 根拠 |
|------|-------|------|
| **DEV の重要性** | ⭐⭐⭐⭐⭐ | 両手法で「第一次分類変数」 |
| **STB の補完性** | ⭐⭐⭐⭐ | 両手法で「第二次条件」として機能 |
| **LIT の必要性** | ❌ | QCA では必須、決定木では表現されず |
| **等結果性** | ❌ | QCA では2経路、決定木では単一経路 |
| **非対称性** | ❌ | QCA では明示的、決定木では暗黙的 |

---

### 🔬 差分が生じた手法的理由（ランキング）

#### 第1位：**サンプルサイズの限界性**
- n=18 は決定木の統計的信頼性の下限
- 決定木は「見かけ上の精度」が高く見えても、実は過学習の可能性
- QCA は本来、少数ケースのための手法設計

#### 第2位：**表現形式の違い**
- QCA：集合論（等結果性・非対称性を自然に表現）
- 決定木：階層型分類（単一経路・対称性を仮定）
- 本データの複雑性（2つの発展経路）を決定木が圧縮

#### 第3位：**変数選択のロジック**
- QCA：すべての変数を並列評価（論理式に組み込む）
- 決定木：再帰的な「最大情報利得」に基づいて逐次選択
- n=18 では、DEV と STB だけが「当面」十分に見え、LIT が後回しに

#### 第4位：**ファジィ化 vs 離散化**
- QCA の連続スコア（0〜1）vs 決定木の二値分岐
- 境界ケースの扱いが異なる

---

### 💡 実務的含意

#### ✅ 決定木から学べることは？
- **DEV ≤ 0.46 の国は危険** → QCA の必要条件「DEV が高いことが必須」と完全一致
- **STB ≥ 0.51 の追加条件** → QCA が「複数条件の組み合わせ」を強調していることの具体化
- 簡潔なルール → ポリシーメーカーへの**コミュニケーション**に有効

#### ⚠️ 決定木だけでは不十分な点
- LIT（識字率）がなぜ重要か説明できない
- なぜ「異なる発展経路」が存在するのか見えない
- n=18 では根拠が不十分（統計検定力が低い）

---

### 🎓 手法の選択に関する提言

#### QCA を優先すべき場合
- 小サンプル（n < 30）
- 複数の因果経路（等結果性）が想定される
- 因果の**非対称性**が理論的に重要（Y と ~Y で異なる条件）
- 条件の**論理的構成**（AND-OR 関係）を理論化したい

#### 決定木を優先すべき場合
- 大サンプル（n > 100）
- **予測精度**が主目的
- 条件間の**相互作用**を視覚的に理解したい
- 一般的な「分類ルール」が必要

#### 本研究データ（Lipset, n=18）
→ **QCA が適切** （決定木は補助的・検証的用途に限定）

---

## 7. 提言される統合アプローチ

決定木とQCAの乖離を埋めるため、以下が提案される：

### 1. **QCA の解に基づいた決定木の構築**
```r
# 代替案：QCA で特定した十分条件を「明示的な条件」にして決定木を構築
# ツリーが自動で選ぶのではなく、理論から条件を事前指定
tree_constrained <- rpart(
  SURV ~ LIT + DEV * STB,  # QCA解に基づく明示的な条件
  data = df_dt,
  control = rpart.control(...)
)
```

### 2. **ブートストラップ安定性の検証**
```r
# 決定木のカットオフ（0.46, 0.51）が複数サンプル再抽出で安定か検証
# n=18 では結果が不安定な可能性を量的に確認
```

### 3. **ファジィ決定木の検討**
```r
# QCA のファジィスコアそのものを決定木に入力し、
# 決定木の分岐点がファジィ化下でも安定か検証
```

---

## 参考：本分析の構造図

```
Lipset 理論（民主主義の前提条件）
    ↓
┌─────────────────────────────────────┐
│     両手法の同時適用（n=18）         │
└────────────┬────────────────────────┘
             ↓
    ┌────────┴────────┐
    ↓                 ↓
  QCA分析         決定木分析
  • 必要条件      • 変数重要度
  • 十分条件      • 分岐ルール
  • 等結果性      • 予測精度
  • 非対称性      
    │                 │
    └────────┬────────┘
             ↓
  ┌──────────────────────────────┐
  │ 一貫性の検証＆乖離の分析      │
  │ → 手法上の違いを理解          │
  │ → 理論的含意を統合            │
  └──────────────────────────────┘
```

